# 近期变更

## 范围

已提交记录覆盖 `main` 分支从 2026-04-14 到 2026-06-15 的 `15a9616a00943ada6c20a0f158e3adb39df2ccac`。日期采用 Git 记录的 author date 和时区。最后一节单独描述截至 2026-07-06 的当前未提交工作区变更，不属于 Git 历史。

## 2026-06-15 17:14:00 -0700 — `15a9616` — 支持批量输出切片

`DroidOutputs` 和 `LiberoOutputs` 将动作切片从 `[:, :N]` 改为 `[..., :N]`。最后一维仍是动作维度，同时保留任意前置 batch 或 horizon 维度。这是一项兼容性扩展；无 batch 维的动作块结果不变。该提交只修改了两行，未增加测试。

## 2026-05-06 00:08:33 +0800 — `c23745b` — 移除冗余客户端依赖

`openpi-client` 移除了无关的 PyPI `tree` 包。因此，lockfile 同时删除了 `tree` 及其传递依赖 `svgwrite`。运行时策略行为未改变，最小客户端环境需要安装的包更少。

## 2026-04-16 11:25:11 +0800 — `650c5b0` — 统一客户端开发依赖

客户端包将 pytest 从旧式 `[tool.uv].dev-dependencies` 移到标准 `dev` dependency group。该变更只影响开发环境安装元数据，不改变运行时依赖集合或 WebSocket 协议。

## 2026-04-15 23:19:43 +0800 — `a190e00` — 将 Chex 加入运行时依赖

根项目将 `chex==0.1.90` 加入运行时依赖并重新生成 lockfile。源代码变更仅涉及依赖声明；较大的 lockfile diff 还刷新了解析后的软件包产物。

## 2026-04-14 21:03:51 -0700 — `e4429ad` — 从模型运行时导入中移除 pytest

PyTorch Gemma 模块不再仅为 cache 类型注解导入 pytest。这样可以防止未安装开发依赖的生产推理镜像在 import 时失败。该变更不影响 cache 或模型计算行为。

## 2026-07-06 — 未提交工作区 — AgiBot G01 π0.5 适配

当前工作区新增仅本地的 LeRobot v2.1 数据路径、G01 机器人变换、全参数微调 π0.5 配置、归一化 CLI 支持、WebSocket 接收超时，以及带受保护动作执行的 ROS2 客户端。同时增加了单元测试和 `examples/agibot_g01` 下的操作文档。

该实现支持直接使用 `task_5093` 训练，将原始 163 维 state 和 36 维 action 数组映射到 16 维双臂策略空间，并向机器人提供 16 步动作块。控制默认禁用。精确映射和验证状态见 [AgiBot G01 实现](implementation/agibot-g01.md)。

当前环境完成的验证包括 Python 3.12 语法编译、`git diff --check`、行长度检查和纯 NumPy 控制安全冒烟检查。由于缺少相关依赖和硬件，未运行完整项目 pytest、LeRobot 解码、JAX/GPU 训练、检查点推理、WebSocket 集成和 ROS2 真机执行。
